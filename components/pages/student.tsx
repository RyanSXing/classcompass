"use client";
import Link from "next/link";
import { ArrowRight, FileText, CalendarDays, UsersRound, Lightbulb, History } from "lucide-react";
import { useWorkspace } from "@/components/workspace-provider";
import { PageGate, EmptyState, Banner } from "@/components/shared";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Card } from "@/components/ui/card";
import { dateLabel, humanize } from "@/lib/utils";
import type { ObservationStatus } from "@/lib/contracts";
const statusLabels:Record<ObservationStatus,string>={independent:'Demonstrated independently',supported:'Demonstrated with support',not_demonstrated:'Not yet demonstrated in this work',insufficient:'Not enough evidence yet',unknown_support:'Support context unknown'};
function StudentContent({studentId}:{studentId:string}){const {data}=useWorkspace();if(!data)return null;const {state,curriculum}=data;const student=state.students.find(s=>s.id===studentId);if(!student)return <div className="page"><EmptyState title="Student not found" text="Choose a student from your classroom roster."/></div>;const observations=state.observations.filter(o=>o.studentId===studentId).sort((a,b)=>b.date.localeCompare(a.date)||b.createdAt.localeCompare(a.createdAt));const current=observations.find(o=>!o.superseded);const submissions=state.submissions.filter(s=>s.studentId===studentId);return <div className="page"><div className="breadcrumb"><Link href="/classroom">Classroom</Link><span>›</span><span>Student evidence</span></div><div className="page-heading"><div className="student-profile"><div className="student-avatar">{student.displayName[0]}</div><div><h1>{student.displayName}’s learning story</h1><p>Fraction addition · Grade 5 · Fictional student</p></div></div><Badge tone="violet">{observations.filter(o=>!o.superseded).length} current observations</Badge></div><div className="progress-layout"><div><div className="section-title" style={{marginTop:0}}><h2>Evidence over time</h2><History size={19} color="#9a88b0"/></div>{observations.length?observations.map(o=>{const first=state.responses.find(r=>r.id===o.evidence[0]?.responseId);const submission=state.submissions.find(s=>s.id===first?.submissionId);return <Card className={`observation ${o.superseded?'superseded':''}`} key={o.id}><div className="observation-header"><Badge tone={o.superseded?'neutral':o.observationStatus==='independent'?'green':o.observationStatus==='supported'?'violet':'amber'}>{o.superseded?'Superseded observation':statusLabels[o.observationStatus]}</Badge><span className="text-small muted">{dateLabel(o.date)}</span></div><h3>{curriculum.templates.find(t=>t.id===o.templateId)?.title??'Classroom observation'}</h3><p>{o.interpretation}</p><div className="observation-meta"><span><CalendarDays size={13} style={{display:'inline',marginRight:5}}/>{dateLabel(o.date,{month:'long',day:'numeric',year:'numeric'})}</span><span>Difficulty: {humanize(o.difficulty)}</span><span>{o.evidence.length} linked answers</span></div>{o.supportSnapshots.map(s=><div className="banner" key={s.submissionId} style={{padding:12,marginTop:10}}><UsersRound size={16}/><div><strong>Recorded help: {humanize(s.support.level)}</strong>{s.support.note&&<p>{s.support.note}</p>}</div></div>)}{o.superseded&&<p className="finding-limit">{o.supersededReason??'Later evidence or a correction changed this interpretation. Kept here so the learning history stays visible.'}</p>}<div className="inline-actions mt-16"><Badge tone="neutral">Next: {humanize(o.suggestedNextStep)}</Badge>{submission&&<Button variant="outline" size="sm" asChild><Link href={`/review/${submission.batchId}?student=${studentId}`}><FileText/>See the original work</Link></Button>}</div></Card>;}):<Card><EmptyState title="A story starts with a piece of work" text={`${student.displayName} doesn’t have teacher-confirmed observations yet. Review uploaded work to begin a dated evidence history.`} action={<Button asChild><Link href={submissions[0]?`/review/${submissions[0].batchId}`:'/classroom'}>Review student work<ArrowRight/></Link></Button>}/></Card>}</div><aside className="spaced"><Card><div className="card-content"><div className="empty-icon" style={{width:42,height:42,borderRadius:12,marginBottom:15}}><Lightbulb size={22}/></div><h3>A useful next step</h3>{current?<><h4 style={{marginTop:15,color:'#6b3db8'}}>{humanize(current.suggestedNextStep)}</h4><p className="muted text-small" style={{marginTop:10}}>Based on the latest reviewed work from {dateLabel(current.date)}. Revisit this suggestion when new evidence arrives.</p></>:<p className="muted text-small" style={{marginTop:12}}>Start with a reviewed response. A blank worksheet or uncertain reading does not define what a student can do.</p>}</div></Card><Banner>These are dated observations, not permanent labels. The task, difficulty, and help provided travel with each piece of evidence.</Banner><Card><div className="card-content"><h3 style={{fontSize:18}}>Learning objectives</h3>{curriculum.objectives.map(o=><div key={o.id} className="constraint-item"><span>•</span><p>{o.description}</p></div>)}</div></Card></aside></div></div>;}
export default function StudentPage({studentId}:{studentId:string}){return <PageGate><StudentContent studentId={studentId}/></PageGate>;}
